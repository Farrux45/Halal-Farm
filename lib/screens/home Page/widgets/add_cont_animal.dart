import 'package:flutter/material.dart';

class AddContAnimals extends StatelessWidget {
  const AddContAnimals({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return GestureDetector(
      
    );
  }
}