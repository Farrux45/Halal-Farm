import 'package:flutter/material.dart';

class ContMyAnimals extends StatelessWidget {
  const ContMyAnimals({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}